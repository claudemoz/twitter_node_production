if tweets
  each tweet in tweets
    div(class="card tweet-element")
      div(class="card-body d-flex flex-column")  
        //- div(class="card-text")=tweet.content  
        div(class="d-flex flex-row justify-content-center")
          img(src=tweet.author.avatar, class="pr-2" width="30" heigth="30")
          span(class="flex-fill")=tweet.author.username
          if editable && tweet.author._id.toString() === currentUser._id.toString()
            div(class="d-flex flex-row justify-content-center")
                a(class="fas fa-edit pr-2 text-success pointer-cursor" href=`/tweets/edit/${tweet._id}`)
                i(class="fas fa-minus-circle text-danger pointer-cursor" tweetId=tweet._id)
        div=tweet.content
        //- button(class="btn btn-danger mt-2 float-right" tweetId=tweet._id) X 
        //- a(class="btn btn-primary mt-2 mr-2 float-right" href=`/tweets/edit/${tweet._id}`) Edit